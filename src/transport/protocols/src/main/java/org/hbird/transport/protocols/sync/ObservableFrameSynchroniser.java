package org.hbird.transport.protocols.sync;

import java.util.Observable;

public abstract class ObservableFrameSynchroniser extends Observable implements FrameSynchroniser {
	// Johannes? Looks like we just want the Observable extension...
}
